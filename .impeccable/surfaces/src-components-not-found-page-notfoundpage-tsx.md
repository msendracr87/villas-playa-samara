---
version: 1
slug: "src-components-not-found-page-notfoundpage-tsx"
primary_target: "src/components/not-found-page/NotFoundPage.tsx"
related_targets: ["src/components/not-found-page/not-found-page.css","src/App.tsx","src/components/header/Header.tsx","src/components/footer/Footer.tsx"]
---

# Custom 404 surface

## Scope and mode

- Scope: `src/components/not-found-page/NotFoundPage.tsx` and its route fallback in `src/App.tsx`.
- Mode: Operate; this is a recovery state inside the established Villas Playa Sámara visual system.

## Visitor job

- Recognize that the requested page does not exist.
- Return home immediately or choose a primary resort section without guessing where to go.

## Direction

- Turn the missing path into a calm return to Sámara through an image-led split shoreline composition.
- Make the oversized 404 the memorable, meaningful marker while the recovery copy and Home action remain primary.
- Preserve the shared Header, Footer, type, color, square geometry, Material Symbols, and interaction behavior.

## Constraints and unresolved decisions

- Utility copy is draft pending owner review.
- Do not add search until site search exists as a real capability.
- Do not infer or publish unresolved business facts.
